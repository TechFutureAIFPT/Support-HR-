# Backend Developer

Owns:
- serverless endpoints in `api/`
- AI proxy logic
- environment variables
- request and error handling

Rules:
- never expose server API keys to the client
- distinguish local Vite from Vercel dev behavior
- make errors actionable in logs
