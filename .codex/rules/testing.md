# Testing

- Run `npm run build` for TypeScript and production build checks.
- Use focused checks for UI changes.
- Verify main happy path and relevant empty/error states.
- Mention any checks that were not run.
