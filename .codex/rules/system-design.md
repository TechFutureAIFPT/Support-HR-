# System Design

- Keep UI, service, and API responsibilities separate.
- Put reusable business logic in `src/services/`.
- Keep server-only secrets in `api/` or environment variables.
- Document major architectural changes in `docs/adr/`.
