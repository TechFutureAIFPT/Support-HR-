# Error Handling

- Show user-facing errors in plain language.
- Log enough context to debug API failures.
- Separate validation, network, quota, and model errors.
- Avoid swallowing errors silently.
