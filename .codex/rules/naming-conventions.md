# Naming Conventions

- Components: `PascalCase`.
- Hooks: `useSomething`.
- Services: domain + purpose, for example `analysisCacheService`.
- Booleans: `is`, `has`, `can`, or `should`.
- Event handlers: `handleAction`.
