# Performance Checklist

- Avoid unnecessary re-renders in large lists.
- Keep heavy AI/file parsing work out of render paths.
- Watch bundle-size warnings after adding dependencies.
- Prefer lazy loading for large feature pages.
