# Testing Patterns

- Build check: `npm run build`.
- API local check: use `npm run dev:full`.
- UI check: verify desktop and mobile states.
- Regression check: repeat the exact user workflow that changed.
